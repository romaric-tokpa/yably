# URL de l’API (local : même port que « Project URL » de `supabase status`)
VITE_SUPABASE_URL=http://127.0.0.1:54321
# Clé « Publishable » ou ancienne clé anon JWT — copiées depuis `supabase start` / `supabase status`
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
